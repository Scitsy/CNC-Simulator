<Window x:Class="FanucSimulator.Stock3DWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="3D View" Height="620" Width="820"
        Background="#0a0a0a" Foreground="#f0f0f0"
        FontFamily="Courier New" ResizeMode="CanResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="3D VIEW (CHUCK + WORKPIECE) - drag to orbit, scroll to zoom"
                   Foreground="{StaticResource ScreenHeader}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>

        <Border Grid.Row="1" Background="#0a0a0a" BorderBrush="#333" BorderThickness="2">
            <Viewport3D x:Name="MainViewport"
                        MouseLeftButtonDown="Viewport_MouseLeftButtonDown"
                        MouseLeftButtonUp="Viewport_MouseLeftButtonUp"
                        MouseMove="Viewport_MouseMove"
                        MouseWheel="Viewport_MouseWheel">
                <Viewport3D.Camera>
                    <PerspectiveCamera x:Name="MainCamera" FieldOfView="45"/>
                </Viewport3D.Camera>
            </Viewport3D>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Reset Orbit" Click="ResetOrbit_Click" Style="{StaticResource PanelButton}"/>
            <Button Content="Refresh" Click="Refresh_Click" Style="{StaticResource PanelButton}" Margin="8,0,0,0"/>
            <Button Content="Close" Click="Close_Click" Style="{StaticResource PanelButton}" Margin="8,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
